
import numpy as np

class Model:
    
    def __init__(self, numWeights, ):
    # x^T * w 
        self.w = np.array(features.length)
    
    def predict(x):
        return x.T @ w
    
    
    
    def loss(self, Y, preds):
        return np.count(np.where(Y[i] == preds[i]))
    def train(X, Y):
        
        #X, Y = Trarin set and labels
        
        
        
        gradients = np.array(M, N,)
        
        i=0
        
        for example in X:
            
            gradients[i] = np.ones(example.shape[0])
            i+=1
            
        
    def predictAllTrue(X):
        return np.ones(X.shape[0])
